export const CardProfile = ({
    avatar,
    profileUrl,
    name,
    userName,
    followers,
    following,
    publicRepos,

}) => {
    return (
        <div>
            {
                name
            }
        </div>
    )
}
